/**
 * Config file returning an object of configuration options
 * which can be relied upon by app, but set in server.js 
 * or a test file prior to calling app
 */
const config = {
  TEST: false,
}

module.exports = config;